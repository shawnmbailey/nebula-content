// Answer the following questions in your own words
    // 1. What is a function?
    //a function is a piece of code that is reusable with labels showing what it's execution should bring

    // 2. Why is it useful?
        //functions can Help our code be more streamline and easier to read, it's like a mini progam that you can combine to
        //make the main program you're building run  

    // 3. Give a 'real world example'. Ours was woodchipper. Try and think of something yourself. 
     real world would be a juicer , you 
    // 4. There is different syntax / ways of defining a function write out the syntax for each:

    //     1. Function declaration:
            function juicer (apples) {
                return apple juice
            }

    //     2. Function expressions:
    let x = function (num) { return num * num };

    //     3. Arrow functions: 
     a => {
        return a + 20
     }
    
    // 5. What is a parameter?
    // the names listed in the function listed inside the function defintion 

    // 6. What is an argument?
   // the values inside your funtion to get you the results 


    // 7. What does the return statement do?
    //it ends the funtion and executes it based on the value you specify 

    // 8. What is scope? Do you have a real-world example of something similar to scope?

a scope is the part of a function where the information can be accessed 